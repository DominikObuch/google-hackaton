# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=nestDb

# MCP Server
MCP_HOST=localhost
MCP_PORT=8123

# Embeddings
EMBEDDING_MODEL=embeddinggemma:300m
EMBEDDING_SERVICE_URL=http://localhost:11434/v1

# Gemini API key (Google AI Studio). Used by: the Settings page connection test (GoogleService),
# the Arena solution generator (GoogleService), and the pipeline module (core problem extraction,
# 5 Whys branch, TRIZ principle translation, LLM-as-judge scoring).
AI_STUDIO_API_KEY=PASTE_YOUR_GEMINI_API_KEY_HERE